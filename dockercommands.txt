sudo docker -v
docker pull mongo
docker run --name=mongo-c1 -p 27017:27017 -d mongo
docker container ls -a
#to restart container if it is stoppped
docker container start <dockerid>
 #docker client without compass
  docker exec -it <dockerid> /bin/bash
  mongosh
  show dbs
  
#to clone nodejs project
git clone -b projects https://github.com/eswaribala/cholanodejsapr2024
#go to registration api folder
docker build -f Dockerfile -t regapp .

#to remove image
docker rmi <imagename>
#install docker compose 
sudo apt install docker-compose
#to list the network
docker network ls
#to create network
docker network create cholanetwork
#pushing images to docker hub
docker tag registrationapi_regapp:latest eswaribala/cholatrainingapr2024:regapp
docker push eswaribala/cholatrainingapr2024:regapp

